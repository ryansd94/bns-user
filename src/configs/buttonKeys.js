import { EButtonType } from 'configs/enums'

export const buttonKey = {
    defaultKeys: [
        EButtonType.add,
        EButtonType.edit,
        EButtonType.delete
    ],
    menu: {
    }
}
import { constants } from "configs"

export const buttonKey = {
    defaultKeys: [
        constants.add_new,
        constants.hidw_show_column,
        constants.edit,
        constants.delete
    ],
    menu: {
    }
}

import { IconCricle } from "components/icon/icon"
import Box from "@mui/material/Box"

const StatusIcon = (props) => {
    const { } = props
    return <Box className="select-item">
        <IconCricle className='notify-status' />
    </Box>
}
export default StatusIcon